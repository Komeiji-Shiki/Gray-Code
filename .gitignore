node_modules
dist
.limcode
.claude

# legacy local-only jest configs (superseded by jest.backend.config.js)
jest.config.cjs
tsconfig.jest.json

# test artifacts
coverage
.jest-cache
.ts-jest
.cache

# lock files (project uses npm)
pnpm-lock.yaml

# logs / runtime artifacts
*.log
*.log.*
logs/
run-logs.zip
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*
tmp-*.out.log
tmp-*.err.log

# packaged extension bundles
*.vsix

# env files (may contain API keys)
.env
.env.*
.env.local
.env.development
.env.production

# private keys / certificates (never commit)
*.pem
*.key
*.p12
*.pfx

# OS metadata
.DS_Store
Thumbs.db
desktop.ini

# Python (fast-tavern py)
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/

# editor swap / backup files
*.swp
*.swo
*~

# JetBrains IDE
.idea/

# TypeScript incremental build info
*.tsbuildinfo

# temp artifacts
.tmp/
diff_analyzer.py
graycode-settings.json
graycode-settings*.json
limcode-settings*.json
splash-preview.html

# local review notes
report.md
docs/

# audit remediation notes (keep local, don't publish)
AUDIT_REMEDIATION.md

# historical planning/research docs (mission complete, keep local)
.graycode/
checkpoint-history-branch-architecture.plan.md

# 独立桌面发行产物与本地运行数据
/release/
/platform-data/
/portable-data/
/local-memory-imports/
*.sqlite
*.sqlite-wal
*.sqlite-shm

# 本地试用、设计讨论和交接记录，保留在工作区，不进入公开源码
/DESKTOP_TRIAL_FEEDBACK.md
/PLATFORM_IMPLEMENTATION_HANDOFF.md
/PLATFORM_MIGRATION_PLAN.md
/PLATFORM_SIMPLIFICATION.md
/STORAGE_ARCHITECTURE_PLAN.md
/BOT_CONVERSATION_PLAN.md
/CHARACTER_CHAT_PLAN.md
/AGENT_TEAMS_REVIEW.md
/frontend/AGENTS.md
/apps/client/AGENTS.md
